const nodeLocation = process.argv[0];
const fileLocation = process.argv[1];
const firstName = process.argv[2];
const lastName = process.argv[3];
 
console.log(`Hello (${nodeLocation}) (${fileLocation}) ${firstName} ${lastName}`);